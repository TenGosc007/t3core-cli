import { Box } from "ink";

import { SettingsView } from "../features/Settings/Settings";

export const Settings = () => {
  return (
    <Box flexDirection="column">
      <SettingsView />
    </Box>
  );
};
